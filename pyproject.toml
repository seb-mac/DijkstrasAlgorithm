[project]
name = "dijkstrasalgorithm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sebmachac2-dev", email = "seb.machac@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "manim>=0.21.0",
    "manim-dsa",
]

[project.scripts]
dijkstrasalgorithm = "dijkstrasalgorithm:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
